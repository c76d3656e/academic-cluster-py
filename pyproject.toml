[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "academic-cluster"
version = "0.1.0"
description = "Academic paper clustering, outline generation, and writing assistance using LangGraph"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [
    { name = "Academic Cluster Team" },
]
keywords = ["academic", "research", "clustering", "langgraph", "ai-writing"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    # LangGraph & LangChain Core
    "langgraph>=0.2.0",
    "langchain-core>=0.3.0",
    "langchain-openai>=0.2.0",
    "langchain-community>=0.3.0",
    "langsmith>=0.2.0",

    # LLM Providers
    "openai>=1.50.0",
    "anthropic>=0.34.0",
    "ollama>=0.4.0",

    # Data Processing
    "pandas>=2.2.0",
    "numpy>=1.26.0",
    "scikit-learn>=1.5.0",

    # Graph & Network Analysis
    "networkx>=3.3",
    "python-igraph>=0.11.0",
    "leidenalg>=0.10.0",

    # Vector Databases
    "chromadb>=0.5.0",
    "qdrant-client>=1.12.0",

    # Database
    "sqlalchemy>=2.0.0",
    "asyncpg>=0.30.0",
    "psycopg2-binary>=2.9.0",
    "alembic>=1.13.0",

    # Redis
    "redis>=5.2.0",
    "aioredis>=2.0.0",

    # HTTP & API
    "httpx>=0.27.0",
    "aiohttp>=3.10.0",
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",

    # NLP & Text Processing
    "spacy>=3.8.0",
    "nltk>=3.9.0",
    "tiktoken>=0.8.0",

    # Embeddings
    "sentence-transformers>=3.0.0",
    "flagembedding>=1.2.0",

    # Clustering & Dimensionality Reduction
    "umap-learn>=0.5.0",
    "hdbscan>=0.8.0",

    # Visualization
    "matplotlib>=3.9.0",
    "seaborn>=0.13.0",
    "plotly>=5.24.0",
    "pyvis>=0.3.0",

    # Configuration & Environment
    "pydantic>=2.9.0",
    "pydantic-settings>=2.5.0",
    "python-dotenv>=1.0.0",
    "toml>=0.10.0",

    # Async & Concurrency
    "anyio>=4.6.0",
    "tenacity>=9.0.0",

    # Logging & Monitoring
    "structlog>=24.4.0",
    "rich>=13.9.0",

    # Testing
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=5.0.0",

    # Utilities
    "python-multipart>=0.0.12",
    "pyyaml>=6.0.0",
    "orjson>=3.10.0",
]

[project.optional-dependencies]
dev = [
    "black>=24.10.0",
    "ruff>=0.8.0",
    "mypy>=1.13.0",
    "pre-commit>=4.0.0",
    "ipykernel>=6.29.0",
    "jupyter>=1.11.0",
]
docs = [
    "mkdocs>=1.6.0",
    "mkdocs-material>=9.5.0",
    "mkdocstrings[python]>=0.26.0",
]

[project.urls]
Homepage = "https://github.com/your-org/academic-cluster"
Documentation = "https://your-org.github.io/academic-cluster"
Repository = "https://github.com/your-org/academic-cluster"
Issues = "https://github.com/your-org/academic-cluster/issues"

[project.scripts]
academic-cluster = "academic_cluster.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/academic_cluster"]

[tool.black]
line-length = 88
target-version = ["py310", "py311", "py312"]

[tool.ruff]
line-length = 88
target-version = "py310"
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
]
ignore = [
    "E501",  # line too long (handled by black)
]

[tool.ruff.isort]
known-first-party = ["academic_cluster"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_functions = ["test_*"]
python_classes = ["Test*"]
addopts = [
    "-ra",
    "-q",
    "--strict-markers",
    "--tb=short",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
    "e2e: marks tests as end-to-end tests",
]
asyncio_mode = "auto"

[tool.coverage.run]
source = ["src/academic_cluster"]
omit = ["*/tests/*", "*/__pycache__/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if __name__ == .__main__.",
    "raise AssertionErrorError",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
]
